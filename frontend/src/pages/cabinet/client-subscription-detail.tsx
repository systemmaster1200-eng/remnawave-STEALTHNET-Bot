import { StealthSubscriptionDetail } from "@/pages/cabinet/stealth/stealth-subscription-detail";

/**
 * Страница конкретной подписки: /cabinet/subscription/:subId.
 * Открывается зелёной кнопкой «Подключить» с карточки подписки.
 */
export function ClientSubscriptionDetailPage() {
  return <StealthSubscriptionDetail />;
}
